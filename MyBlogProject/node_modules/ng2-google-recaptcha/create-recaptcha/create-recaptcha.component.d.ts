import { OnInit, EventEmitter } from '@angular/core';
import { RenderRecaptchaDirective } from '../render-recaptcha/render-recaptcha.directive';
export declare class CreateRecaptchaComponent implements OnInit {
    onCaptchaComplete: EventEmitter<any>;
    siteKey: string;
    captchaStyle: any;
    renderDelay: number;
    recaptchaId: string;
    recaptchaInstance: RenderRecaptchaDirective;
    ngOnInit(): void;
    resetRecaptcha(): void;
    onCaptchaCompleted(data: string): void;
}
